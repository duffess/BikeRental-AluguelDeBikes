<UserControl x:Class="SeuProjeto.Views.BikeManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="20" Background="WhiteSmoke">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Text="Gerenciamento de Bicicletas" FontSize="28" FontWeight="Bold" Foreground="#333" Margin="0,0,0,15"/>

        <!-- Formulário para cadastro/edição -->
        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,10,390,0">
            <TextBlock Text="Modelo:" FontWeight="SemiBold" Margin="0,5"/>
            <TextBox Name="txtModel" Height="30" Margin="0,0,0,10" />

            <TextBlock Text="Marca:" FontWeight="SemiBold" Margin="0,5"/>
            <TextBox Name="txtBrand" Height="30" Margin="0,0,0,10" />

            <TextBlock Text="Ano:" FontWeight="SemiBold" Margin="0,5"/>
            <TextBox Name="txtYear" Height="30" Margin="0,0,0,10" />

            <Button Content="Salvar" Height="35" Background="#007ACC" Foreground="White" />
        </StackPanel>

        <!-- DataGrid para listar bicicletas -->
        <DataGrid Grid.Row="1" Margin="370,0,0,0" AutoGenerateColumns="False" IsReadOnly="False"
                  CanUserAddRows="True" CanUserDeleteRows="True" HeadersVisibility="Column"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Modelo" Binding="{Binding Model}" Width="*"/>
                <DataGridTextColumn Header="Marca" Binding="{Binding Brand}" Width="*"/>
                <DataGridTextColumn Header="Ano" Binding="{Binding Year}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
 